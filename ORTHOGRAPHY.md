# Panduan Ejaan Baku Kamus Bajau Samah (Official Orthography Guide)

Dokumen ini menetapkan standard rasmi ejaan, fonetik, dan morfofonemik bagi projek **Kamus Bajau Samah**. Standard ini diasaskan daripada konsensus linguistik tempatan (*Persatuan Seni Budaya Bajau Samah* [PSBB/USBO] & Mark T. Miller, 2007) serta diselaraskan untuk kegunaan digital moden.

---

## 1. Sistem Hentian Glotal (*Glottal Stop* `/ʔ/` $\longrightarrow$ `'`)

1. **Hentian Glotal Akhir Kata**:
   * Ditulis dengan tanda apostrof lurus (`'`).
   * Membezakan pasangan perkataan berbeza maksud (*minimal pairs*):
     * `ta` *(kata tugas)* $\longleftrightarrow$ **`ta'`** *(di / pada [lokatif])*
     * `bo` *(dan / lalu)* $\longleftrightarrow$ **`bo'`** *(buluh)*
     * `bu'e'` *(air)*, `sedo'` *(ikan)*, `ngenda'` *(melihat)*, `keta'` *(lintas)*, `duri'` *(duri)*, `bua'` *(mulut / buah)*.
2. **Konsonan Velar `/k/` Tulen vs Glotal `'`**:
   * Huruf **`k`** hanya digunakan untuk hentian velar bersuara tidak berhembus tulen:
     * `anak` *(anak)*, `luuk` *(lauk)*, `suk` *(kurus / sempit)*, `kakan` *(makan)*.
   * Huruf **`'`** digunakan untuk semua hentian glotal:
     * `bu'e'`, `sedo'`, `ngenda'`, `keta'`.
3. **Hentian Glotal Antara Vokal (*Intervocalic Glottals*)**:
   * Pasangan vokal dengan hentian glotal di tengah mengekalkan tanda `'` sebagai dua suku kata terpisah:
     * `a'a` *(orang / manusia)* $\longrightarrow$ `/aʔa/` *(bukan /aː/)*
     * `o'o` *(ya)*, `le'e'` *(tempat itu)*.
4. **Peleburan Glotal Semasa Pengimbuhan (*Glottal Elision*)**:
   * Apabila kata dasar yang berakhir dengan glotal menerima akhiran vokal (`-an` / `-on`), glotal lebur dan memanjangkan vokal:
     * `bara'` + `-an` $\longrightarrow$ **`baraan`** `/baˈraːn/` *(memberitahu kepada)*.

---

## 2. Sistem Diftong (`-ai` dan `-au`)

Bagi memudahkan pembacaan masyarakat dan selaras dengan Ejaan Rumi Melayu, semua perkataan berakhiran geluncuran diftong dieja secara seragam dengan **`-ai`** dan **`-au`** (bukan `-ay` / `-aw`):

| Kata Baku | Ejaan Lama (Fonetik) | Maksud Melayu | Transkripsi IPA |
| :--- | :--- | :--- | :--- |
| **`parai`** | *paray* | padi | `/paraj/` |
| **`kerabau`** | *kerabaw* | kerbau | `/kərabaw/` |
| **`pantau`** | *pantaw* | berdiri | `/pantaw/` |
| **`ellau`** | *ellaw* | hari, siang | `/əlːaw/` |
| **`matai`** | *matay* | mati | `/mataj/` |
| **`patai`** | *patay* | bunuh | `/pataj/` |
| **`kadai`** | *kaday* | kedai, pekan | `/kadaj/` |
| **`sampai`** | *sampay* | sampai, tiba | `/sampaj/` |
| **`tilau`** | *tilau* | tanya | `/tilaw/` |
| **`duai`** | *duai* | turun | `/du.a.i/` |

---

## 3. Sistem Vokal & Tanda Aksen (`e` vs `é`)

Bahasa Bajau Samah mempunyai 6 fonem vokal. Untuk membezakan vokal taling dan pepet secara tepat di peringkat antarabangsa:

1. **Huruf `e` (Pepet `/ə/`)**:
   * Mewakili vokal tengah schwa (seperti *e* dalam *emak*, *sedap*):
     * `emma'` `/əmːaʔ/` *(bapa, ayah)*
     * `sedo'` `/sədoʔ/` *(ikan)*
     * `keta` `/kəta/` *(lintas)*
     * `beli` `/bəli/` *(beli)*
2. **Huruf `é` dengan Aksen Tirus (Taling `/e/` / `/ɛ/`)**:
   * Mewakili vokal depan separuh sempit (seperti *e* dalam *élok*, *bécak*):
     * `bése'` `/beseʔ/` *(air mata)*
     * `léko'` `/lekoʔ/` *(ular)*
     * `kérabau` `/kerabaw/` *(kerbau)*
3. **Vokal Panjang Phonemik (Geminat `aa`, `ii`, `oo`, `uu`, `ee`)**:
   * Huruf berkembar melambangkan vokal panjang `/ː/`:
     * `laan` `/laːn/` *(jalan)*
     * `raat` `/raːt/` *(buruk)*
     * `tingkoo'` `/tiŋkoːʔ/` *(duduk)*
     * `too` `/toː/` *(benar / kanan)*
     * `tuun` `/tuːn/` *(turun)*
     * `luuk` `/luːk/` *(lauk)*

---

## 4. Konsonan Kembar / Geminasi (`ll`, `mm`, `pp`, `tt`, `kk`, `bb`)

Konsonan kembar dikekalkan kerana ia adalah fonemik dan membezakan intonasi serta makna:
* **`emma'`** *(bapa / ayah)*
* **`ellau`** *(hari / siang)*
* **`ellu`** *(lesung)*
* **`sikki'`** *(sempit)*
* **`tappuk`** *(sembunyi)*
* **`kallang`** *(karang / menyanyi)*
* **`pettung`** *(buluh betung)*

---

## 5. Bentuk Standard Kata "Air"

* Kata dasar standard rasmi bagi *air* ialah: **`bu'e'`**
* Variasi dialek tempatan direkodkan dalam medan `dialects`:
  * **`boe'`** *(ejaan lama / santai)*
  * **`bohe'`** *(variasi Semporna / Pantai Timur)*

---

## 6. Penulisan Partikel Berglotal

Partikel tugas pendek sentiasa mengekalkan tanda hentian glotal `'`:
* **`ko'`** *(dan / dengan)*
* **`bo'`** *(kemudian / lalu)*
* **`ta'`** *(di / pada [kata sendi nama])*
* **`le'`** *(oleh [kata sendi pelaku])*
* **`nya'`** *(tidak / bukan)*

---

## 7. Penulisan Klitik & Imbuhan

1. **Kata Ganti Nama Pemilikan / Pelaku**:
   * Ditulis dengan **tanda sengkang (`-`)** untuk kejelasan morfologi:
     * `oron-nu` *(nama kamu)*
     * `ruma'-ku` *(rumah saya)*
     * `betis-nu` *(betis kamu)*
     * `dela-ni` *(lelakinya)*
2. **Klitik Aspek & Wacana (`no`, `jo`, `po`)**:
   * Ditulis **bercantum terus** dengan kata sebelumnya:
     * `tilauno` *(tanyalah)*
     * `boino` *(sudah)*
     * `turino` *(tidurlah)*
     * `manganjo` *(makanlah)*
     * `turipo` *(masih tidur)*
3. **Awalan Asli Bajau Samah**:
   * Sentiasa **`be-`** *(bukan ber-)*: `bedenakan`, `beluuk`, `bejomo`.
   * Sentiasa **`te-`** *(bukan ter-)*: `tekito`, `teturi`, `tekuri'`.

---

## 8. Harmoni Vokal Akhiran & Apitan (`-an` $\longrightarrow$ `-on`)

Apabila vokal terakhir kata dasar ialah `/o/`, akhiran dan apitan bertukar kepada **`-on`**:
* `jomo` $\longrightarrow$ **`pejomoon`** *(bukan pejomoan)*, **`jomoon`**
* `songom` $\longrightarrow$ **`kesongomon`**, **`pesongomon`**
* `kebo` $\longrightarrow$ **`keboon`**
* `tingkoo'` $\longrightarrow$ **`tingkoo'on`**
* `sorong` $\longrightarrow$ **`sorongon`**

Untuk vokal lain (`a`, `i`, `e`, `u`), akhiran kekal dengan **`-an`**:
* `beli` $\longrightarrow$ **`belian`**, `bagi` $\longrightarrow$ **`bagian`**, `alap` $\longrightarrow$ **`kealapan`**.
